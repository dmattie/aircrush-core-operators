from util import data_transfer
import sys

project='abide'
subject='28880'
session='1'

data_transfer.commit(project=project,subject=subject,session=session)
#data_transfer.commit(project=project,subject=subject)
